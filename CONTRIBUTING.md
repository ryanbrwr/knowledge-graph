# Contributing

1. Open an issue for changes to the public model or validation semantics.
2. Add or update a domain-neutral example and a focused test.
3. Run `npm run check`.
4. Keep provider, persistence, UI, and curriculum-specific code outside this repository.

Bug fixes and documentation improvements can be submitted directly as pull requests. Public API changes should explain the migration path and preserve deterministic behavior.
